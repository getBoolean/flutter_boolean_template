name: store
description: Offline-first database and file storage
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  stock: ^1.0.1

dev_dependencies:
  mocktail: ^0.3.0
  test: ^1.19.2
  flutter_lints: ^2.0.0
