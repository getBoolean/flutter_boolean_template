name: logs
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  leak_tracker: ^2.0.1
  logger: ^1.2.2
  logger_flutter_plus: ^1.0.5
  logging: ^1.1.1
  riverpod: ^2.2.0

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
