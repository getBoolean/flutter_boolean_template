import 'package:flutter/widgets.dart';

class FeatureFirst extends StatelessWidget {
  const FeatureFirst({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
